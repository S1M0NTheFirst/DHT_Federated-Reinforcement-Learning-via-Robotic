Bootstrap: docker
From: nvidia/cuda:11.8.0-cudnn8-runtime-ubuntu22.04

# Image for Conditions B, C, D, E. No flower/kademlia — these workers run a
# random policy. Includes criu so Conditions C and D can dump processes
# inside the container if rootless CRIU works on this kernel.

%post
    export DEBIAN_FRONTEND=noninteractive
    apt-get update && apt-get install -y \
        python3.10 python3-pip \
        criu \
        git wget curl iproute2 net-tools rsync openssh-client \
        && rm -rf /var/lib/apt/lists/*

    pip3 install --no-cache-dir --upgrade pip setuptools wheel

    pip3 install --no-cache-dir torch==2.1.0 \
        --index-url https://download.pytorch.org/whl/cu118

    pip3 install --no-cache-dir \
        psutil==5.9.8 pynvml==11.5.0 redis==5.0.1 \
        numpy==1.24.4 pandas==2.0.3 matplotlib==3.7.2

    mkdir -p /app /checkpoints

%environment
    export PYTHONUNBUFFERED=1
    export TF_CPP_MIN_LOG_LEVEL=3
    export GRPC_VERBOSITY=ERROR
    export OMP_NUM_THREADS=4
    export MKL_NUM_THREADS=4

%runscript
    exec python3 "$@"
